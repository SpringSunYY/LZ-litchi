<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>litchi</artifactId>
        <groupId>com.lz.litchi</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>litchi-module-mall</artifactId>
    <packaging>pom</packaging>

    <name>${project.artifactId}</name>

    <description>
        商城大模块，由 product 商品、promotion 营销、trade 交易、statistics 统计等组成
    </description>
    <modules>
        <module>litchi-module-product</module>
        <module>litchi-module-promotion</module>
        <module>litchi-module-trade</module>
        <module>litchi-module-statistics</module>
        <!--
            特殊：为什么会有 litchi-module-trade-api 呢？
                litchi-module-promotion 和 litchi-module-trade 之间相互循环依赖，所以抽出 litchi-module-trade-api 模块，这样：
                1. litchi-module-promotion 依赖 litchi-module-trade-api
                2. litchi-module-trade 依赖 litchi-module-promotion
            从而不存在相互（循环）依赖，即 litchi-module-trade => litchi-module-promotion => litchi-module-trade-api
         -->
        <module>litchi-module-trade-api</module>
    </modules>

</project>
